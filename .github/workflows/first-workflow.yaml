name: First_workflow
on: push
jobs:
    test-job:
        runs-on: ubuntu-latest
        steps:
            - name: Test job welome message
              run: echo "Executing the test"
            - name: Redme faile content
              run: cat README.md
          